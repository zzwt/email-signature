import React from 'react';
import type { NextPage } from 'next';
import Head from 'next/head';

const Home: NextPage = () => (
  <div>
    <Head>
      <title>Sigmailer</title>
      <meta name="description" content="Sigmailer" />
      <link rel="icon" href="/favicon.ico" />
    </Head>
    404 Error
  </div>
);

export default Home;
